<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.doublez.system.mapper.UserMapper">

    <select id="selectUserList" resultType="com.doublez.system.domain.user.vo.UserVO">
        SELECT
            user_id,
            nick_name,
            sex,
            phone,
            email,
            wechat,
            school_name,
            major_name,
            introduce,
            status
        FROM
            tb_user
        <where>
            <if test="query.userId !=null ">
                user_id = #{query.userId}
            </if>
            <if test="nickName !=null and nickName != ''">
                AND nick_name like concat('%', #{query.nickName}, '%')
            </if>
        </where>
        ORDER BY
            create_time DESC
    </select>
</mapper>